[project]
name = "api-toolkit"
version = "0.1.0"
description = "Lightweight REST API framework with middleware support"
requires-python = ">=3.8"
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"
